format = 1
id = "spring.core"
version = "1.0.0"
requires_core = "^1"

[exports]
macros = ["component", "service", "repository", "configuration", "bean", "qualifier", "primary"]
commands = []

[[capabilities]]
name = "applications.resolved_target"
required = true
